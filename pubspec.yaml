name: kaizen_app
description: "KAIZEN - Tu app de mejora continua"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.24.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.13.0
  firebase_analytics: ^10.7.0
  google_sign_in: ^6.2.0
  flutter_riverpod: ^2.4.10
  riverpod_annotation: ^2.4.0
  go_router: ^13.0.0
  google_fonts: ^6.1.0
  connectivity_plus: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  riverpod_generator: ^2.3.9
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
